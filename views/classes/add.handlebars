<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elearn</title>
</head>
<body>
    {{#if user}}
        <h2 style="font-weight: 600; color:rgb(0, 255, 183)">Create Class</h2>
        <form id="clsForm" method="post" action="http://localhost:3000/classes/add" style="margin-top: 50px">
            <div>
			    <label style="font-size: 25px; font-weight: 600; color: rgb(0, 255, 183)">Title: </label>
			    <input type="text" name="title" value="{{title}}" style="width: 70%; height: 35px" placeholder="Enter class's title here...">
		    </div><br/>
            <div>
			    <label style="font-size: 25px; font-weight: 600; color: rgb(0, 255, 183); margin-top: -320px">Description: </label>
			    <textarea id="textarea1" name="description" value="{{description}}" style="width: 70%; height: 350px; font-size: 15px; white-space: pre;" placeholder="Enter class's description here..."></textarea>
		    </div><br />
		<div>
			<input type="submit" value="Add Class" style="background-color: rgb(255, 137, 3); font-weight: bold; margin-left: 180px">
		</div>
        </form>
    {{else}}
	    <p>user must be logged in to add more classes</p>
    {{/if}}
</body>
</html>